name: Build and Push Docker image

on:
  push:
    branches:
      - main  # или master, или любая ветка, с которой хочешь деплоить

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: denstep123/allures-backend:latest
    - name: 🔁 Вызов вебхука на сервере
      run: |
        curl -X POST "${{ secrets.SERVER_WEBHOOK_URL }}" \
        -H "X-GitHub-Secret: ${{ secrets.SOME_SECRET_HEADER }}"
